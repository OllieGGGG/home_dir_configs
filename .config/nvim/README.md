Steps to install this config:
1. Clone repo to ~/.config/nvim/.
2. Install [nmvim packer](https://github.com/wbthomason/packer.nvim).
3. Install ripgrep(for grep files).

Hints:
* Install **xclip**(Xorg), **wl-clipboard**(wayland) to be able to copy to system clipboard.

